"""RL training integrations for AgentArk.

`ark_rl` owns framework adapters and training-facing entrypoints. Runtime Unity
process management stays in `agent_ark.ark_env`; evaluation flows stay in
`agent_ark.ark_eval`.
"""
